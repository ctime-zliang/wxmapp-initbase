export enum ECustomTabbarKey {
	Home = 'Home',
	Personal = 'Personal',
}

export enum EPersonalCardKey {}

export enum ERemoteDataLoadStatus {
	static,
	loading,
	loaded,
	loadfail,
}

export enum ERemoteDataUploadStatus {
	static,
	uploading,
	uploaded,
	uploadfail,
}
